# LLM Handoff

Canonical working copy: [../LLM_HANDOFF.md](../LLM_HANDOFF.md)

This `docs/` file is intentionally a small pointer to avoid two handoff documents drifting out of sync.

**Note:**
The canonical `LLM_HANDOFF.md` in the repo root includes:

- Current architecture and module ownership after mission/main refactor completion
- Build and packaging commands/outputs
- Current build-size realities and optimization priorities
- Validation workflow for future changes
- Automated airport smoke workflow (`pytest -m airport_smoke` and `scripts/run_airport_smoke.ps1`)

Always update the root handoff file, not this pointer.
